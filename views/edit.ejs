<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Entry - DSA Journal</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-gradient-to-br from-zinc-900 via-zinc-800 to-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 hover:bg-red-600 transition-colors rounded-md px-3 py-2 text-sm mb-5 inline-block"
                href="/profile">Back to Profile</a>
        </div>
        <h3 class="text-3xl mb-5">Edit Entry</h3>
        <form method="POST" action="/update/<%= post._id %>">
            <input type="text" name="title" value="<%= post.title %>" required
                class="block w-1/3 mb-3 px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-lg">
            <textarea name="content" required
                class="block w-1/3 h-40 mb-3 px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-lg"><%= post.content %></textarea>
            <button class="px-5 py-2 rounded-md bg-blue-500 hover:bg-blue-600 transition-colors">
                Update Entry
            </button>
        </form>
    </div>
</body>

</html>