<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-gradient-to-br from-zinc-900 via-zinc-800 to-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 hover:bg-red-600 transition-colors rounded-md px-3 py-2 text-sm mb-5 inline-block"
                href="/logout">Logout</a>
        </div>
        <div class="flex items-start gap-3 bg-zinc-800/50 p-5 rounded-lg backdrop-blur-sm">
            <div class="w-16 h-16 bg-red-500 rounded-full overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt="">
            </div>
            <div>
                <h3 class="text-3xl"><span class="font-light text-zinc-400">Welcome back,</span>
                    <%= user.name %>
                </h3>
                <p class="text-zinc-500">@<%= user.username %>
                </p>
            </div>
        </div>

        <div class="mt-10">
            <h5 class="mb-5 text-xl font-light">Create a new post</h5>
            <form method="POST" action="/post">
                <textarea placeholder="What's on your mind?"
                    class="block p-4 outline-none w-1/3 resize-none bg-zinc-800/50 border-2 border-zinc-700 rounded-lg backdrop-blur-sm"
                    name="content"></textarea>
                <input class="px-4 py-2 text-sm bg-blue-500 hover:bg-blue-600 transition-colors block rounded-md mt-3"
                    type="submit" value="Create new post">
            </form>
        </div>
        <div class="postcontainer mt-8">
            <% user.posts.reverse().forEach(function(post) { %>
                <div
                    class="post w-1/3 p-5 mb-4 rounded-lg border-[1px] border-zinc-700/50 bg-zinc-800/50 backdrop-blur-sm hover:bg-zinc-800/70 transition-colors">
                    <h4 class="text-blue-500 mb-2">
                        <%= user.username %>
                    </h4>
                    <p class="text-sm tracking-tight">
                        <%= post.content %>
                    </p>
                    <small class="mt-2 inline-block">
                        <%= post.likes.length %> likes
                    </small>
                    <div class="btns flex gap-4 mt-1">
                        <a class="text-blue-500" href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id)===-1 ? "Like" : "Unlike" %>
                        </a>
                        <a class="text-zinc-600" href="/edit/<%=post._id %>">edit</a>
                    </div>
                </div>
                <% }) %>
        </div>
    </div>
</body>

</html>