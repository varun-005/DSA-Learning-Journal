<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - DSA Journal</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <h3 class="text-3xl mb-5">Login Account</h3>
        <form action="/login" method="post">
            <% if (typeof message !=='undefined' ) { %>
                <div class="text-green-500 mb-4">
                    <%= message %>
                </div>
                <% } %>
                    <input class="block w-1/3 mb-3 px-3 py-2 outline-none bg-transparent border-2 border-zinc-800"
                        type="email" placeholder="email" name="email">
                    <input class="block w-1/3 mb-3 px-3 py-2 outline-none bg-transparent border-2 border-zinc-800"
                        type="password" placeholder="password" name="password">
                    <input class="px-5 py-2 rounded-md bg-blue-500 hover:bg-blue-600 transition-colors" type="submit"
                        value="Login">
        </form>
        <p class="mt-3 text-sm">
            Don't have an account? <a class="text-blue-500" href="/">Register</a>
        </p>
    </div>

</body>

</html>